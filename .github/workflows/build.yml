name: "🏗️ Build Plugin"

on:
  push:
    branches:
      - master
      - main
      - fix-unreleased
    paths-ignore:
      - "**/*.md"
  pull_request:
    branches:
      - master
      - main
    paths-ignore:
      - "**/*.md"
  workflow_dispatch:

jobs:
  call:
    uses: jellyfin/jellyfin-meta-plugins/.github/workflows/build.yaml@master
    with:
      dotnet-version: "9.0.*"
      dotnet-target: "net9.0"
